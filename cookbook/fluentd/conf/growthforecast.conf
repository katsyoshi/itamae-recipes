<match map.dstat.**.*>
  type forest
  subtype growthforecast
  <template>
    gfapi_url http://yourhostname:5125/api/
    graph_path ${tag_parts[2]}/${tag_parts[3]}/${key_name}
    remove_prefix map.dstat
    name_key_pattern .*
  </template>
</match>
